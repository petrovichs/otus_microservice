apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-ch.fullname" . }}-test-connection"
  labels:
    {{- include "hello-ch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-ch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
